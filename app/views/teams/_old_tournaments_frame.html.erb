<turbo-frame id="old-tournaments">
  <h4 class="text-l text-center py-2">Турниры до рейтинга</h4>
  <div class="py-2">
    <% cache "old_team_tournaments_list/#{@model_name}/#{@team.id}" do %>
      <%= render "old_tournaments_list" %>
    <% end %>
  </div>
</turbo-frame>
